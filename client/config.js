// Global configuration for the frontend
const CONFIG = {
  // Leave empty for local development, or set to the backend URL for production
  // Example: 'https://dfus-backend.onrender.com'
  API_BASE_URL: 'https://dfus.onrender.com'
};
